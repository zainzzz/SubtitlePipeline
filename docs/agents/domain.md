# Domain Docs

## Layout

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root.

## Consumer skills

Skills that read domain context: `diagnosing-bugs`, `tdd`, `improve-codebase-architecture`.

These skills look for:
- `CONTEXT.md` at repo root
- `docs/adr/*.md` for architectural decisions
