// Test setup loaded by vitest before each test file (see vitest.config.ts).
// Pulls in jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.) so
// React component tests can use them without per-file imports.

import '@testing-library/jest-dom/vitest'
