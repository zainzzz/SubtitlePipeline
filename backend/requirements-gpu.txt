fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
httpx==0.28.1
openai==2.11.0
huggingface_hub==0.35.3

# GPU-accelerated PyTorch (CUDA 12.6)
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.8.0+cu126
torchaudio==2.8.0+cu126
torchvision==0.23.0+cu126

# whisperx 3.8.6 is the latest stable release (no 4.x exists as of 2025-07).
# Updated from 3.8.5 to pick up bug fixes and CVE patches.
whisperx==3.8.6
faster-whisper==1.2.0
# Pinned to exact version to avoid surprise breaking changes from
# transformers pre-release churn.
transformers==4.57.6
librosa==0.11.0
qwen-asr==0.0.6
